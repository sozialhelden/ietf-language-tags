// This is to ensure all types that have an external use are actually exported

import FetchCache, {
  CachedValue,
  Config,
  ICachedValueWithState,
  IMandatoryOptions,
  IMinimalResponse,
  IOptionalOptions,
  Options,
  State,
  TTLFunction,
} from './index';
